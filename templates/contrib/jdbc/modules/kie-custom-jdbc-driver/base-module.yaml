schema_version: 1

name: kie-custom-jdbc-driver
execute:
- script: install.sh

envs:
  - name: "DRIVER_DIR"
    value: "${DRIVER_DIR}"
  - name: "JDBC_ARTIFACT"
    value: "${DRIVER_JDBC_ARTIFACT_NAME}"