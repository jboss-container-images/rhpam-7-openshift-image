schema_version: 1

# Purpose: Artifact overrides are for replacing "non-plain" artifacts with
#          "plain" artifacts. Useful, for example, when referenced modules have
#          not updated from "path" to "name". See "Artifact types" here:
#          https://docs.cekit.io/en/latest/descriptor/module.html#artifact-types
#          Currently used for jenkins builds for continuous integration.
#   Usage: cekit build --overrides-file branch-overrides.yaml \
#                      --overrides-file artifact-overrides.yaml

artifacts:
  - name: jbeap-30487.zip
    url: https://download.eng.pnq.redhat.com/released/JBoss-middleware/eap7/7.4.23/patches/jbeap-30487.zip
    md5: db5c1bd8e6df2bd273affccd8c63719a

